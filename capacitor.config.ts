import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AAbad_Examen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
